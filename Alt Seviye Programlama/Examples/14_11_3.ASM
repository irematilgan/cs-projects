        PAGE 60,80
        TITLE ornek____
        
CODESG  SEGMENT PARA 'CODE'
        ORG 100H
        ASSUME CS:CODESG, SS:CODESG, DS:CODESG, ES:CODESG
BILGI:  JMP BASLA
ARR 	DW -2,7,-2,6,5,4
COUNT	DW 4,0,4,1,2,3
SONUC	DW 6 DUP(0)
ELEMAN	DW 6

BASLA   PROC NEAR
	XOR SI, SI
	MOV CX, ELEMAN

L1:
	MOV BX, COUNT[SI]
	SHL BX, 1
L3:	CMP SONUC[BX], 0
	JE DEVAM
	ADD BX,2
	JMP L3
DEVAM:
	MOV AX, ARR[SI]
	MOV SONUC[BX], AX
	ADD SI,2
	LOOP L1

	
        RET
BASLA   ENDP
CODESG  ENDS
        END BILGI
